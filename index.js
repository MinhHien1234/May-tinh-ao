import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Import tệp CSS
import Calculator from './Calculator'; // Import Calculator component

ReactDOM.render(
  <React.StrictMode>
    <Calculator />
  </React.StrictMode>,
  document.getElementById('root')
);
